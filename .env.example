# SQLite (développement) — valeur par défaut
DATABASE_URL=sqlite:///./data/sgiq.db

# PostgreSQL (production)
# DATABASE_URL=postgresql://user:password@localhost:5432/sgiq

# SMTP (optionnel — aussi configurable dans Paramètres → E-mail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre@email.com
# SMTP_PASSWORD=mot_de_passe_application
# SMTP_FROM=votre@email.com
# SMTP_TLS=true
